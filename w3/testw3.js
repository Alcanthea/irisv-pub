const showOnPage = function (text) {
    let newParagraph = document.createElement("p")
    newParagraph.innerHTML = text
    let outputDiv = document.getElementById("output")
    outputDiv.append(newParagraph)
  }

  
//let dailyChoice = function (fullTime) {
   // let startTime = 8
    
   // if(startTime = startTime) {
     //  return `It is ${fullTime} a.m time for work! Remember to relax and not stress about it!`
   // }

//}
//let result = dailyChoice(8)
//showOnPage(result)


//let emailTime = function (emailCheck) {
    //let timeForEmail = 10

    
    //if(timeForEmail = timeForEmail) {
      // return `It is ${emailCheck} a.m. Alright time to check on emails. Lets Go!`
   // }

//}
//let resultTwo = emailCheck(10)
//showOnPage(resultTwo)

// Daily Decision
// Refactored for Week 3
// What to eat for lunch today, and get it by 12 PM when lunch starts
// For myself and my brother (we both work at home)

